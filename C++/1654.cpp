#include<bits/stdc++.h>

#define endl '\n'

using namespace std;

int lan[10005];
int K, N;

int bisearch()
{
	
	return 0;
}

int main(void)
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int tmp;
	int sum = 0;

	cin >> K >> N;
	for(int i = 0; i < K; ++i)
		cin >> lan[i];

	sort(lan, lan+K);

	return 0;
}