#include<iostream>
#define endl '\n'

using namespace std;

int dp[1005];
int X[1005];

int main(void)
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int n;
	cin >> n;
	for(int i = 0; i < n; ++i)
		cin >> X[i];

	for(int i = 0; i < n; ++i)
	{
		
	}

	return 0;
}